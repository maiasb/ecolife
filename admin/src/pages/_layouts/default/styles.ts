import { styled } from '@mui/material'

export const Container = styled('div')(() => ({
  height: '100%',
}))
export const Content = styled('div')(() => ({
  paddingLeft: '300px',
  height: '100vh',
}))
