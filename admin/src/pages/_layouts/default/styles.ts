import { styled } from '@mui/material'

export const Container = styled('div')(() => ({
  height: '100%',
}))
export const Content = styled('div')(() => ({
  display: 'flex',
  paddingLeft: '300px',
  marginRight: '5px',
  height: '100vh',
  overflow: 'hidden',
}))
